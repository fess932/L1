package l1_test

import (
	"testing"
)

// Разработать программу, которая перемножает, делит, складывает,
// вычитает две числовых переменных a,b, значение которых > 2^20.

func bigCalc(s string) string {
	return ""
}

func Test_bigCalc(t *testing.T) {
	bigCalc("abc")
}
